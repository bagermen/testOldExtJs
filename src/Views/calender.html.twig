{{ '{' }}
    "cid":{{ item.cid }},
    "year":"{{ item.year }}",
    "name":"{{ item.name }}",
    "city":"{{ item.city }}",
    "country":"{{ item.country }}",
    "begin":"{{ item.begin | date("Y-m-d") }}",
    "tot":"{{ item.tot | date("Y-m-d") }}",
    "holder":{{ item.holder }},
    "status":{{ item.status ? 'true':'false' }},
    "logo":"{{ item.logo | raw }}",
    "type":"{{ item.type }}",
    "game":"{{ item.game }}",
    "note":"{{ item.note }}",
    "url":"{{ item.url | raw }}",
    "contactName":"{{ item.contactName }}",
    "contactEmail":"{{ item.contactEmail }}",
    "contactPhone":"{{ item.contactPhone }}",
    "name_ru":"{{ item.nameRu }}",
    "ip":"{{ item.ip }}",
    "recognition":"{{ item.recognition }}",
    "wcupStars":{{ item.wcupStars }},
    "isResult":"{{ item.isResult }}",
    "liveUrl":"{{ item.liveUrl | raw }}",
    "tourLogo":"{{ item.tourLogo }}",
    "datesSure":{{ item.datesSure ? 'true':'false' }},
    "placeSure":{{ item.placeSure ? 'true':'false' }},
    "tempo":"{{ item.tempo }}",
    "isOpen":{{ item.isOpen ? 'true':'false' }},
    "fee":{{ item.fee }},
    "isYouth":{{ item.isYouth ? 'true':'false' }},
    "ageСategories":"{{ item.ageСategories }}",
    "addedDate":"{{ item.addedDate | date("Y-m-d H:i:s") }}"
{{ '}' }}